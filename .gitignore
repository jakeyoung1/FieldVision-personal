# Secrets — never commit
.streamlit/secrets.toml

# Pre-computed embeddings (large file, recomputed on deploy)
data/embeddings.npy

# Python
__pycache__/
*.pyc
.env

# Local env
.venv/
.claude-flow/

# Decision audit log (local runs; production points FV_AUDIT_DB at a volume)
fieldvision_audit.db
*.db-journal
